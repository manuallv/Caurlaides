<section class="mx-auto max-w-2xl">
  <div class="shell-card p-8 text-center">
    <p class="text-sm font-semibold uppercase tracking-[0.2em] text-brand-700">404</p>
    <h1 class="mt-3 text-3xl font-extrabold text-slate-900">Page not found</h1>
    <p class="mt-3 text-sm leading-6 text-slate-500">The page you requested does not exist or is no longer available.</p>
    <div class="mt-6">
      <a href="<%= currentUser ? '/dashboard' : '/login' %>" class="btn-primary">Go back</a>
    </div>
  </div>
</section>
