<section class="shell-card overflow-hidden">
  <div class="border-b border-slate-200/80 px-5 py-5 sm:px-6">
    <h1 class="text-2xl font-bold text-slate-900">Audit log</h1>
    <p class="mt-1 text-sm text-slate-500">Event-level activity with actor and timestamp history.</p>
  </div>

  <div class="space-y-4 px-5 py-5 sm:px-6">
    <% if (!activity.length) { %>
      <p class="text-sm text-slate-500">No audit entries yet.</p>
    <% } %>
    <% activity.forEach((entry) => { %>
      <article class="rounded-2xl border border-slate-200 px-4 py-4">
        <div class="flex flex-col gap-2 sm:flex-row sm:items-center sm:justify-between">
          <div>
            <p class="text-sm font-semibold text-slate-900"><%= entry.message %></p>
            <p class="mt-1 text-xs uppercase tracking-wide text-slate-400">
              <%= entry.actor_name || 'System' %> · <%= entry.entity_type %> · <%= entry.action %>
            </p>
          </div>
          <p class="text-sm text-slate-500"><%= helpers.formatDateTime(entry.created_at) %></p>
        </div>
      </article>
    <% }) %>
  </div>
</section>
